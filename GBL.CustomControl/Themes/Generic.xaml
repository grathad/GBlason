<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:GBL.CustomControl">
    <Style TargetType="{x:Type local:ShapeViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ShapeViewer}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Path x:Name="GeometryDrawing" Data="{Binding Path=Geometry, RelativeSource={RelativeSource TemplatedParent}}"
                              Fill="{Binding Path=Fill, RelativeSource={RelativeSource TemplatedParent}}">
                            <Path.RenderTransform>
                                <ScaleTransform ScaleX="{Binding Path=ScaleX, RelativeSource={RelativeSource TemplatedParent}}" ScaleY="{Binding Path=ScaleY, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </Path.RenderTransform>
                        </Path>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
